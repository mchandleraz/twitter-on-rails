<ul>
	<% @tweets.each do |tweet| %>
		<li class="tweet">
			<div class="tweet__user">
				<img src="<%= tweet['user']['profile_image_url'] %>" />
				<p><%= tweet['user']['name'] %></p>
			</div>
			<%= tweet["text"] %>
		</li>
	<% end %>
</ul>